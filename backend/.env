# Database
MONGO_URI=mongodb://localhost:27017/cardo

# JWT Secret
JWT_SECRET=cardo_jwt_secret_20241220120000

# Email delivery: Disable forced dev mode so SMTP can be used
FORCE_DEV_EMAIL=false

# Keep development until SMTP is fully configured (so OTP shows in UI if send fails)
NODE_ENV=development

# === Gmail SMTP (Recommended) ===
# Set these and create a Gmail App Password (16 chars) under Google Account > Security > App Passwords
USE_GMAIL=true
SMTP_USER=alexantony2026@mca.ajce.in
SMTP_PASS=uwls rfmf hobi mowt
SMTP_SECURE=true
SMTP_PORT=465
MAIL_FROM=alexantony2026@mca.ajce.in

# === Alternative: Generic SMTP ===
# Uncomment and set if you prefer a custom SMTP provider
# SMTP_HOST=smtp.example.com
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_SECURE=true
# SMTP_PORT=465
# MAIL_FROM=your-from@example.comzz